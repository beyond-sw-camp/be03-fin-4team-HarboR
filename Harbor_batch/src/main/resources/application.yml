server:
  port: 8005
spring:
  application:
    name: batch
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    #    url: jdbc:mariadb://som_db/som?allowPublicKeyRetrieval=true&useSSL=false
    url: jdbc:mariadb://localhost:3306/harbor_employee
    username: root
    password: 1234 # 1234
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MariaDBDialect
    generate-ddl: true
    hibernate:
      ddl-auto: update #update
    show-sql: true
  batch:
    #    job:
    #      names: ${job.name:NONE}
    jdbc:
      initialize-schema: ALWAYS
